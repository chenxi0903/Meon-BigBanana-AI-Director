# Tasks

- [x] Task 1: Update `PromptManager.tsx` to use correct full IDs.
  - [ ] Rename `buildDetailedKeyframeOptPrompt` to `buildDetailedKeyframeOptimizationPrompt`.
  - [ ] Rename `buildDetailedActionSugPrompt` to `buildDetailedActionSuggestionPrompt`.
- [x] Task 2: Verify synchronization logic.
  - [ ] Ensure that `getAllSystemPrompts` will now correctly map the database records to these UI items.

# Task Dependencies
- Task 2 depends on Task 1.
